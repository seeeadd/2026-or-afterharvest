# 2026 outreach pages

One folder per lead, built by `scripts/build_site.py` in the Outreach Thumbnail Factory and pushed here.
Vercel serves each folder as `/<slug>`. Pages carry noindex: they are concept previews sent to one person.
